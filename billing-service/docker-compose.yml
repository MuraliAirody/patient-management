services:
  billing-service:
    image: billing-service:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: billing-service
    ports:
      - "4001:4001"
      - "9001:9001"
    networks:
      - internal

networks:
  internal:
    external: true
